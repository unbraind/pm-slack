{
  "name": "pm-slack",
  "version": "2026.6.14",
  "description": "Slack notifications for pm item lifecycle events",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unbraind/pm-slack.git"
  },
  "bugs": {
    "url": "https://github.com/unbraind/pm-slack/issues"
  },
  "homepage": "https://github.com/unbraind/pm-slack#readme",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "manifest.json",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build && npm run build:test && node --test dist-test/*.js",
    "dev": "tsc --watch",
    "prepack": "npm run build",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck",
    "audit:prod": "npm audit --omit=dev",
    "pack:dry-run": "npm pack --dry-run",
    "changelog": "pm-changelog --pm-root .agents/pm --mode prepend --output CHANGELOG.md --release-version-from-package --since-previous-tag --until-release-tag --item-url-base https://github.com/unbraind/pm-slack/blob/main/.agents/pm",
    "changelog:full": "pm-changelog --pm-root .agents/pm --mode replace --output CHANGELOG.md --all-release-tags --release-version-from-package --item-url-base https://github.com/unbraind/pm-slack/blob/main/.agents/pm",
    "changelog:check": "pm-changelog --pm-root .agents/pm --mode replace --output CHANGELOG.md --all-release-tags --release-version-from-package --item-url-base https://github.com/unbraind/pm-slack/blob/main/.agents/pm --check",
    "release:check": "npm run typecheck && npm run build && npm test && npm run audit:prod && npm run pack:dry-run && npm run changelog:check",
    "prepublishOnly": "npm run release:check",
    "release:notes": "pm-changelog --pm-root .agents/pm --stdout --since-previous-tag --until-release-tag --release-version-from-package --item-url-base https://github.com/unbraind/pm-slack/blob/main/.agents/pm --pm-bin ./node_modules/.bin/pm --github-step-summary"
  },
  "peerDependencies": {
    "@unbrained/pm-cli": ">=2026.6.7"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "@unbrained/pm-cli": "^2026.6.9",
    "pm-changelog": "^2026.6.13",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "extension",
    "notifications",
    "pm-cli",
    "pm-extension",
    "pm-package",
    "slack"
  ],
  "license": "MIT",
  "author": "@unbraind",
  "pm": {
    "aliases": [
      "slack"
    ],
    "extensions": [
      "."
    ],
    "catalog": {
      "display_name": "Slack Notifications",
      "category": "notifications",
      "summary": "Slack notifications for pm item lifecycle events",
      "tags": [
        "slack",
        "notifications",
        "hooks"
      ],
      "links": {
        "docs": "https://github.com/unbraind/pm-slack#readme",
        "npm": "https://www.npmjs.com/package/pm-slack",
        "repository": "https://github.com/unbraind/pm-slack",
        "report": "https://github.com/unbraind/pm-slack/issues"
      }
    },
    "docs": [
      "README.md",
      "CHANGELOG.md"
    ],
    "examples": [
      "README.md"
    ]
  },
  "publishConfig": {
    "access": "public"
  }
}
